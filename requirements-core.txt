# Core runtime for the MVP / easiest local mode.
# This profile keeps the app local-first and does not require Chroma extras.

# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sse-starlette==2.2.1
httpx==0.28.1

# Data parsing
openpyxl==3.1.5
pdfminer.six==20231228
beautifulsoup4==4.12.3
python-docx>=1.1.0

# Retrieval / storage
numpy
diskcache==5.6.3
aiosqlite==0.20.0

# Configuration
pydantic-settings==2.7.1

# Embeddings
# Install torch separately if your environment does not already provide it.
sentence-transformers==3.3.1
