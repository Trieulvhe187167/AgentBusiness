# Local RAG Agent - Product-like configuration
# Copy this file to .env and adjust values.

# -------------------------------
# Core paths / runtime
# -------------------------------
# RAG_DATA_DIR=data
# RAG_SQLITE_PATH=data/metadata.db

# -------------------------------
# Embeddings
# -------------------------------
RAG_EMBEDDING_MODEL=all-MiniLM-L6-v2
# For multilingual retrieval:
# RAG_EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2
# Higher-quality multilingual (heavier):
# RAG_EMBEDDING_MODEL=intfloat/multilingual-e5-large
# Or:
# RAG_EMBEDDING_MODEL=BAAI/bge-m3

# Offline embedding model path (optional)
# RAG_EMBEDDING_MODEL_PATH=models/all-MiniLM-L6-v2

RAG_EMBEDDING_BATCH_SIZE=64
# Optional manual prefixes (auto-detected for e5/bge if left empty)
# RAG_EMBEDDING_QUERY_PREFIX=
# RAG_EMBEDDING_PASSAGE_PREFIX=

# -------------------------------
# Vector backend
# -------------------------------
# chroma (recommended) | numpy (fallback)
RAG_VECTOR_BACKEND=chroma
RAG_CHROMA_COLLECTION_NAME=kb_chunks

# Use this only when connecting to an external Chroma server
# RAG_CHROMA_HTTP_URL=http://localhost:8000

# -------------------------------
# Retrieval / mode thresholds
# -------------------------------
RAG_TOP_K=10
RAG_MIN_SIMILARITY_THRESHOLD=0.30
RAG_THRESHOLD_GOOD=0.60
RAG_THRESHOLD_LOW=0.40

# Auto thresholds used only when sentence-transformers is unavailable
RAG_HASHING_MIN_SIMILARITY_THRESHOLD=0.12
RAG_HASHING_THRESHOLD_GOOD=0.32
RAG_HASHING_THRESHOLD_LOW=0.18

# -------------------------------
# Answer mode
# -------------------------------
# auto | extractive | generative
RAG_ANSWER_MODE=auto

# auto | openai | gemini | ollama | llama_cpp | openai_compatible | none
RAG_LLM_PROVIDER=openai_compatible

# -------------------------------
# Agent runtime contract (Phase 0 baseline)
# -------------------------------
# vllm | qwen_agent | sglang | ollama | llama_cpp | openai | gemini | custom
RAG_AGENT_SERVING_STACK=vllm

# manual_json | openai_tools
RAG_AGENT_TOOL_PROTOCOL=manual_json

# Keep native tool calling disabled until the manual JSON agent loop is stable.
RAG_AGENT_NATIVE_TOOL_CALLING=false

# auto | required | none
RAG_AGENT_TOOL_CHOICE_MODE=auto

# Optional future parser for native tool calling, for example:
# RAG_AGENT_TOOL_PARSER=qwen3_coder

# -------------------------------
# OpenAI Compatible (product path)
# -------------------------------
RAG_LLM_BASE_URL=http://127.0.0.1:8000/v1
RAG_LLM_API_KEY=EMPTY
RAG_LLM_MODEL=Qwen/Qwen3-4B-Instruct-2507

# -------------------------------
# OpenAI (product path)
# -------------------------------
# RAG_OPENAI_API_KEY=
RAG_OPENAI_MODEL=gpt-4o-mini
RAG_OPENAI_BASE_URL=https://api.openai.com/v1

# -------------------------------
# Gemini (product path)
# -------------------------------
# RAG_GEMINI_API_KEY=
RAG_GEMINI_MODEL=gemini-2.0-flash
RAG_GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# -------------------------------
# Ollama (local path)
# -------------------------------
# RAG_OLLAMA_BASE_URL=http://localhost:11434
RAG_OLLAMA_MODEL=qwen2.5:3b

# -------------------------------
# llama.cpp (local path)
# -------------------------------
# RAG_LLM_MODEL_PATH=models/qwen2.5-1.5b-instruct-q4_k_m.gguf
RAG_LLM_N_CTX=2048
RAG_LLM_MAX_TOKENS=768
RAG_LLM_TEMPERATURE=0.25
RAG_LLM_TOP_P=0.9

# -------------------------------
# Chunking
# -------------------------------
RAG_CHUNK_SIZE=1000
RAG_CHUNK_OVERLAP=120

# -------------------------------
# Upload + cache
# -------------------------------
RAG_MAX_UPLOAD_SIZE_MB=50
RAG_CACHE_TTL_SECONDS=3600
RAG_CACHE_MAX_SIZE_MB=500

# -------------------------------
# Session / logs
# -------------------------------
RAG_SESSION_TTL_MINUTES=120
RAG_CHAT_LOG_LIMIT_DEFAULT=50

# -------------------------------
# External business integrations
# -------------------------------
RAG_INTEGRATION_CACHE_TTL_SECONDS=120
RAG_INTEGRATION_HTTP_TIMEOUT_SECONDS=15

# Order API
# Example:
# GET http://your-order-api/orders/status?order_code=DH12345&user_id=user-1
# GET http://your-order-api/orders/recent?user_id=user-1&limit=5
# RAG_ORDER_API_BASE_URL=http://127.0.0.1:9001
# RAG_ORDER_API_KEY=
RAG_ORDER_API_STATUS_PATH=/orders/status
RAG_ORDER_API_RECENT_PATH=/orders/recent

# Game API
# Example:
# GET http://your-game-api/alliances/online?alliance_id=LM01&server_id=S1
# RAG_GAME_API_BASE_URL=http://127.0.0.1:9002
# RAG_GAME_API_KEY=
RAG_GAME_API_ONLINE_PATH=/alliances/online

# -------------------------------
# Logging
# -------------------------------
RAG_LOG_LEVEL=INFO
